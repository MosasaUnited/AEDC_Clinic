import 'package:flutter/material.dart';

class MonthlyTreatmentViewBody extends StatelessWidget {
  const MonthlyTreatmentViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
