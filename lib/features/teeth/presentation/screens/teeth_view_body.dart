import 'package:flutter/material.dart';

class TeethViewBody extends StatelessWidget {
  const TeethViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
